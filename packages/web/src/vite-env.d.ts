/// <reference types="vite/client" />

// Defined via vite.config.ts → `define` from packages/web/package.json's
// `version` field. Read at module evaluation, not runtime, so a build
// bakes the literal in.
declare const __APP_VERSION__: string
